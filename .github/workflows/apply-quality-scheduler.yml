name: Apply quality scheduler

on:
  pull_request:
    branches:
      - main
    paths:
      - '.agent/apply_quality_scheduler.py'
      - 'tools/quality-scheduler-smoke.html'
      - '.github/workflows/apply-quality-scheduler.yml'

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/consolidate-quality-scheduler

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Apply focused migration
        run: |
          python .agent/apply_quality_scheduler.py
          rm .agent/apply_quality_scheduler.py

      - name: Refresh runtime registry
        run: npm run runtime:registry

      - name: Validate project
        run: npm run validate

      - name: Verify UI actions smoke
        run: npm run smoke:ui-actions

      - name: Upload smoke failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: quality-scheduler-migration-failure
          path: browser-smoke-failure.log
          if-no-files-found: error
          retention-days: 3

      - name: Verify changed files
        run: |
          node --check assets/js/app.js
          node --check tools/run-ui-actions-smoke.mjs
          node --check tools/validate-runtime-architecture.mjs
          git diff --check

      - name: Commit verified changes
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add assets/js/app.js tools/run-ui-actions-smoke.mjs tools/quality-scheduler-smoke.html tools/validate-runtime-architecture.mjs data/runtime-modules.json .agent/apply_quality_scheduler.py
          if git diff --cached --quiet; then
            echo 'No changes to commit.'
          else
            git commit -m 'perf: consolidate quality check timer'
            git push origin HEAD:agent/consolidate-quality-scheduler
          fi

# История изменений

## 3.84.0

- Усилена проверка телефона в `assets/js/quality.js`.
- Контроль качества теперь считает критичной ошибкой номер, который выглядит неполным или ошибочным при включённых контактах или отрывных телефонах.
- Добавлена подсказка, если в поле телефона есть лишний текст, ухудшающий читаемость отрывных листков.
- `assets/js/preprintSummary.js` теперь отдельно предупреждает о неполном номере перед печатью.
- Добавлены безопасные прямые действия для QR, фото и канала отклика: включённые блоки не отключаются автоматически, а пользователь переводится к нужному полю.
- `assets/js/qualityExtraActions.js` теперь сразу показывает для пустого QR действие «Добавить ссылку» и больше не содержит прямого выключения QR через `disableQr`.
- `assets/js/qualityExtraActions.js` теперь также ведёт к полям загрузки пустых фото через `focusPhotoOne` и `focusPhotoTwo`, а `quality.js` больше не создаёт старые действия `noPhoto` и `onePhoto`.
- `assets/js/qualityExtraActions.js` теперь обрабатывает отсутствие канала отклика через прямое действие `responseChannel`, а `quality.js` больше не создаёт старое действие `showContact` для этого замечания.
- `assets/js/qrIntentFix.js` удалён: временная страховка больше не нужна, потому что пустой QR теперь обрабатывается напрямую в `qualityExtraActions.js`.
- `assets/js/photoIntentFix.js` удалён: временная страховка больше не нужна, потому что пустые фото теперь обрабатываются напрямую в `qualityExtraActions.js`.
- `assets/js/responseChannelPhoneGuard.js` удалён: временная страховка больше не нужна, потому что канал отклика теперь обрабатывается напрямую в `qualityExtraActions.js`.
- Мягкая QR-подсказка теперь работает только для 4 макетов и не дублирует предупреждение для мини-макетов 6–8.
- Подсказка плотного макета с фото теперь различает 4 макета и 6–8 макетов на А4.
- Проверка `validate:layout-media-preservation` теперь отдельно контролирует текст фото-подсказки для 6 и 8 макетов.
- Проверка `validate:layout-media-preservation` теперь контролирует сохранение всех включённых режимов фото: `one`, `two` и `plan`.
- Проверка `validate:layout-media-preservation` теперь берёт список включаемых режимов фото из `assets/js/state.js`, а не из ручного списка.
- Проверка `validate:layout-media-preservation` теперь проверяет все включённые режимы фото не только в `auto`, но и в явных режимах подстройки.
- Проверка `validate:layout-media-preservation` теперь контролирует, что мягкая подстройка сохраняет сами данные `photoOne` и `photoTwo`.
- Проверка `validate:layout-media-preservation` теперь контролирует, что мягкая подстройка сохраняет данные QR: `qrLink` и `qrCaption`.
- Проверка `validate:layout-media-preservation` теперь контролирует, что мягкая подстройка не отключает включённый QR даже при пустой ссылке.
- Проверка `validate:layout-media-preservation` теперь проверяет пустой включённый QR не только в `auto`, но и в явных режимах подстройки.
- Подавленный QR-дубль больше не учитывается в фильтре, сводке, приоритете, состоянии кнопки печати и сводке перед печатью.
- Проверка changelog теперь безопасно выделяет только текущий раздел версии и не опирается на неподходящий для JavaScript якорь `\z`.
- Проверки прямых действий QR, фото и канала отклика теперь связывают `action:null` с конкретным замечанием, чтобы старые действия `disableQr`, `noPhoto`, `onePhoto` и `showContact` не вернулись под изменённым текстом.
- Проверка package-скриптов теперь точно разбирает общий `npm run validate`, ловит лишние команды, дубли и нарушение порядка validate-скриптов.
- Проверка связей файлов теперь ловит повторное подключение локальных `script` и `stylesheet`-ассетов в HTML, чтобы один модуль не запускался дважды.
- Добавлена проверка `validate:asset-duplicates`, которая закрепляет защиту от возврата дублей HTML-ассетов.
- Проверка README теперь контролирует описание `validate:asset-duplicates` и его подключение в `package.json`.
- Добавлена отдельная кнопка «Подстроить, сохранив фото и QR» для мягкой автоподстройки без отключения уже включённых медиа.
- Подсказки плотного макета теперь прямо предлагают мягкую подстройку, когда пользователь хочет сохранить фото или QR.
- README теперь уточняет, что мягкая автоподстройка сохраняет фото и QR в `auto` и явных режимах подстройки.
- Чек-лист регрессии теперь фиксирует выбранный принцип автоподстройки: старое автоисправление не меняется, мягкое действие доступно отдельно.
- Чек-лист регрессии теперь также фиксирует явные режимы мягкой автоподстройки и защиту исходного состояния макета от мутаций.
- Добавлена проверка `validate:layout-media-preservation`, которая защищает мягкую автоподстройку фото и QR.
- Проверка `validate:layout-media-preservation` теперь проверяет реальное поведение обычной и мягкой автоподстройки на перегруженном макете.
- Проверка `validate:layout-media-preservation` теперь также покрывает явные режимы подстройки и защищает исходное состояние макета от мутаций.
- Добавлены документы `docs/quality-helper-map.md` и `docs/quality-regression-checklist.md` для сопровождения helper-модулей качества и ручной регрессии.
- README обновлён под текущую цепочку helper-модулей качества, а workflow теперь запускает проверки при изменении `docs/**` и `README.md`.
- Добавлены проверки `validate:asset-duplicates`, `validate:layout-media-preservation`, `validate:photo-intent-action`, `validate:response-channel-action`, `validate:qr-empty-direct-action`, `validate:suppressed-quality-items`, `validate:quality-helper-imports`, `validate:quality-helper-map`, `validate:quality-regression-checklist` и `validate:readme-quality-docs`.
- Версия состояния и пакета обновлена до `3.84.0`.

## 3.83.0

- Обновлён `README.md` под текущее состояние проекта.
- В структуру добавлены `assets/js/layoutExtras.js`, `assets/js/qualityExtraActions.js` и все актуальные `tools/validate-*.mjs` проверки.
- В описание возможностей добавлены единый модуль расширенных полей, быстрые исправления контроля качества и расширенный набор проверок проекта.
- Раздел проверки проекта теперь перечисляет все отдельные `npm run validate:*` команды.
- Версия состояния и пакета обновлена до `3.83.0`.

## 3.82.0

- Добавлена проверка `tools/validate-layout-extras.mjs` для расширенных полей макета.
- Проверка контролирует, что ключи хранения призыва, подписи отрывных листков и брендовой строки описаны только в `assets/js/layoutExtras.js`.
- `npm run validate` теперь запускает проверку `validate:layout-extras` вместе с остальными проверками проекта.
- Это защищает проект от повторного размножения одинаковых `localStorage`-ключей по разным JS-файлам.
- Версия состояния и пакета обновлена до `3.82.0`.

## 3.81.0

- В `assets/js/layoutExtras.js` добавлена общая функция записи расширенных полей макета.
- Редакторы `spnContactEditor.js`, `spnTearOffEditor.js` и `spnBrandEditor.js` больше не хранят собственные ключи `localStorage` для призыва, подписи и брендовой строки.
- `assets/js/qualityExtraActions.js` теперь использует общий helper для быстрых исправлений и сохраняет прежнее поведение: поле раскрывается, получает фокус и макет обновляется.
- Это завершает перенос записи и чтения расширенных полей макета в один общий модуль.
- Версия состояния и пакета обновлена до `3.81.0`.

## 3.80.0

- `assets/js/layoutExtrasSync.js` переведён на общий модуль `assets/js/layoutExtras.js`.
- `assets/js/preprintSummary.js` теперь берёт призыв, подпись отрывных листков и брендовую строку через общий модуль.
- В `assets/js/layoutExtras.js` добавлено чтение “сырого” значения без запасного текста, чтобы сводка перед печатью могла отличать пустое поле от дефолта.
- Это завершает объединение расширенных полей макета вокруг одного источника правил.
- Версия состояния и пакета обновлена до `3.80.0`.

## 3.79.0

- Добавлен общий модуль `assets/js/layoutExtras.js` для расширенных полей макета.
- Сохранение макетов, скачивание JSON, рендер предпросмотра и проверка качества теперь используют один общий источник правил для призыва, подписи отрывных листков и брендовой строки.
- Это снижает риск, что одно из мест будет показывать или сохранять старое значение после будущих доработок.
- Поведение существующих макетов сохранено: рендер по-прежнему не подменяет сохранённый текст дефолтным значением из видимого редактора.
- Версия состояния и пакета обновлена до `3.79.0`.

## 3.78.0

- Добавлена проверка `tools/validate-changelog.mjs` для истории изменений.
- Проверка контролирует, что версии в `docs/changelog.md` не дублируются.
- Проверка также контролирует, что версии идут сверху вниз по убыванию.
- `npm run validate` теперь запускает проверку changelog вместе с остальными проверками проекта.
- Версия состояния и пакета обновлена до `3.78.0`.

## 3.77.0

- Добавлена проверка `tools/validate-package-scripts.mjs` для команд проверки в `package.json`.
- Проверка контролирует, что каждый `validate:*` скрипт включён в общий `npm run validate`.
- Проверка также контролирует, что `validate:*` команды запускают существующие файлы `tools/*.mjs`.
- Это снижает риск добавить новую проверку и забыть подключить её к общей проверке проекта.
- Версия состояния и пакета обновлена до `3.77.0`.

## 3.76.0

- Добавлена проверка `tools/validate-version-sync.mjs` для синхронизации версии проекта.
- Проверка сравнивает версию в `package.json`, `assets/js/state.js` и верхнюю запись в `docs/changelog.md`.
- `npm run validate` теперь запускает проверку синхронизации версий вместе с остальными проверками.
- Это снижает риск выпустить обновление с разъехавшимися версиями в коде и документации.
- Версия состояния и пакета обновлена до `3.76.0`.

## 3.75.0

- Добавлена проверка `tools/validate-storage-safety.mjs` для защиты браузерных сохранений.
- Проверка контролирует, что важные операции `localStorage` в `assets/js/storage.js` имеют безопасные значения возврата при ошибках.
- Проверка также контролирует, что `assets/js/app.js` показывает пользователю понятные сообщения при ошибках сохранения профиля, макетов и избранного.
- `npm run validate` теперь запускает новую проверку вместе с остальными проверками проекта.
- Версия состояния и пакета обновлена до `3.75.0`.

## 3.74.0

- Усилена работа с профилем СПН, удалением сохранённых макетов и избранными шаблонами.
- `assets/js/storage.js` теперь безопасно обрабатывает ошибки `localStorage` для профиля, удаления макета и переключения избранного.
- `assets/js/app.js` показывает понятные статусы, если профиль, удаление макета или избранное не удалось сохранить из-за ограничений браузера.
- Это продолжает защиту интерфейса от повреждённых или переполненных браузерных сохранений.
- Версия состояния и пакета обновлена до `3.74.0`.

## 3.73.0

- Улучшена обработка ошибок сохранения макетов в `assets/js/app.js` и `assets/js/storage.js`.
- Кнопка «сохранить последний макет» теперь показывает понятное сообщение, если браузер не смог сохранить данные.
- Именованные макеты теперь не ломают интерфейс при ошибке `localStorage`; пользователь получает статус о невозможности сохранить макет.
- Сохранение именованного макета стало устойчивее к старым или повреждённым записям без имени.
- Версия состояния и пакета обновлена до `3.73.0`.

## 3.72.0

- Усилена работа с последним сохранённым макетом в `assets/js/storage.js`.
- Сохранение последнего макета теперь защищено от ошибок `localStorage` и возвращает признак успеха.
- Загрузка последнего макета теперь безопасно обрабатывает повреждённые данные в браузере и возвращает пустой результат вместо сбоя интерфейса.
- Версия состояния и пакета обновлена до `3.72.0`.

## 3.71.0

- Улучшена загрузка сохранённых макетов в `assets/js/app.js`.
- Последний сохранённый макет и выбранный сохранённый макет теперь загружаются от чистого базового состояния.
- Это предотвращает смешивание старых данных текущего макета с неполными сохранениями из старых версий.
- Импорт JSON переиспользует общий безопасный путь загрузки состояния.
- Версия состояния и пакета обновлена до `3.71.0`.

## 3.70.0

- Улучшено открытие JSON-файлов макетов в `assets/js/app.js`.
- Файл теперь загружается от чистого базового состояния, а не накладывается поверх текущего макета.
- Это предотвращает смешивание старых данных объекта с импортированным макетом, если файл неполный или создан в старой версии.
- После открытия файла список сохранённых макетов визуально снимает выбор.
- Версия состояния и пакета обновлена до `3.70.0`.

## 3.69.0

- Улучшена кнопка «Очистить объект» в `assets/js/app.js`.
- При очистке объекта теперь сбрасывается старое название макета, чтобы новый объект не сохранялся под прежним названием.
- Список сохранённых макетов визуально снимает выбор после очистки объекта.
- Имя, телефон СПН и расширенные настройки контактов при этом сохраняются.
- Версия состояния и пакета обновлена до `3.69.0`.

## 3.68.0

- Улучшены быстрые исправления в `assets/js/qualityExtraActions.js`.
- После исправления поле автоматически раскрывается, получает фокус и прокручивается в видимую область.
- Исправление брендовой строки теперь показывает пользователю поле внутри блока «Гибкая настройка для опытных», а не меняет его незаметно.
- Версия состояния и пакета обновлена до `3.68.0`.

## 3.67.0

- Добавлена проверка `tools/validate-quality-actions.mjs` для быстрых исправлений контроля качества.
- `npm run validate` теперь проверяет, что заголовки быстрых исправлений из `qualityExtraActions.js` реально существуют в `quality.js`.
- Проверка также контролирует подключение редакторов контактов, отрывных листков, бренда и модуля быстрых исправлений в `index.html`.
- Версия состояния и пакета обновлена до `3.67.0`.

## 3.66.0

- Добавлен модуль `assets/js/qualityExtraActions.js` для быстрых исправлений новых замечаний контроля качества.
- В контроле качества появились кнопки для исправления призыва в контактах, подписи отрывных телефонов и брендовой строки.
- Быстрые исправления записывают значения в расширенные редакторы, обновляют предпросмотр и повторно запускают проверку качества.
- `index.html` подключает новый модуль после редакторов контактов, отрывных листков и бренда.
- Версия состояния и пакета обновлена до `3.66.0`.

## 3.65.0

- Улучшена сводка макета перед печатью в `assets/js/preprintSummary.js`.
- В сводке теперь показываются призыв в контактном блоке, подпись отрывных листков и брендовая строка.
- Перед печатью добавлены предупреждения для пустого призыва, длинной подписи отрывных листков и перегруженной брендовой строки при плотной печати.
- Версия состояния и пакета обновлена до `3.65.0`.

## 3.64.0

- Расширена проверка качества макета в `assets/js/quality.js`.
- Контроль качества теперь учитывает призыв в контактном блоке.
- Добавлены предупреждения для длинной подписи отрывных листков.
- Добавлены предупреждения для перегруженной брендовой строки в плотной печати.
- Частный режим теперь проверяет фирменность с учётом брендовой строки.
- Версия состояния и пакета обновлена до `3.64.0`.

## 3.63.0

- Улучшена проверка связей файлов в `tools/validate-assets.mjs`.
- Проверка теперь проходит по JS-файлам, подключённым из HTML, а не только по цепочке от `assets/js/app.js`.
- Добавлена поддержка side-effect import вида `import './module.js';`.
- Новый импорт `layoutExtrasSync.js` теперь тоже попадает в штатную проверку связей.
- Версия состояния и пакета обновлена до `3.63.0`.

## 3.62.0

- Добавлен модуль `assets/js/layoutExtrasSync.js` для восстановления расширенных настроек при импорте JSON-макета.
- `preprintSummary.js` подключает синхронизацию расширенных полей без изменения разметки `index.html`.
- При открытии JSON-файла восстанавливаются призыв в контактах, подпись отрывных телефонов и брендовая строка.
- После импорта предпросмотр автоматически обновляется, чтобы макет показывал восстановленные значения.
- README обновлён с учётом нового модуля.
- Версия состояния и пакета обновлена до `3.62.0`.

## 3.61.0

- Расширенные настройки макета добавлены в базовое состояние: призыв в контактах, подпись отрывных телефонов, текст рядом с логотипом и правая брендовая подпись.
- Автосохранение, сохранение последнего макета и именованные макеты теперь сохраняют расширенные настройки.
- Скачиваемый JSON-файл макета теперь дополняется расширенными настройками из видимых редакторов и localStorage.
- README обновлён с учётом сохранения расширенных настроек.
- Версия состояния и пакета обновлена до `3.61.0`.

## 3.60.0

- Подключены дополнительные помощники на главной странице генератора.
- Добавлен пошаговый маршрут работы для СПН: задача, шаблон, данные, фото/QR, печать, проверка.
- Добавлены редакторы контактного блока, подписи отрывных телефонов и брендовой строки.
- Добавлены блоки задания на расклейку и отчёта после расклейки.
- Подключенные помощники переведены на `type="module"`, чтобы служебные функции разных файлов не конфликтировали между собой.
- README обновлён с учётом новых рабочих блоков.
- Версия состояния и пакета обновлена до `3.60.0`.

## 2.3.0

- Добавлен пакет шаблонов `data/templates_entrance.json` для расклейки по подъездам.
- Добавлены шаблоны для точечного поиска квартир в конкретном доме, верхнего и нижнего этажа, спроса по дому, продажи объекта соседям, аренды и оценки квартиры в доме.
- Загрузчик шаблонов теперь подключает четыре файла: базовый, расширенный, локальный Борисоглебск и подъездный пакет.
- Версия состояния обновлена до `2.3.0`.

## 2.2.0

- Добавлен локальный пакет шаблонов `data/templates_borisoglebsk.json`.
- Добавлены шаблоны под Борисоглебск: центр, Аэродромная, Северный район, частный сектор, дома, новостройки, оценка, покупатели и частные объявления.
- Загрузчик шаблонов теперь подключает три файла: базовый, расширенный и локальный.
- Локальные шаблоны можно найти через поиск по словам: Борисоглебск, центр, Аэродромная, Северный, дом, новостройки.
- Версия состояния обновлена до `2.2.0`.

## 2.1.0

- Добавлен файл `data/templates_extra.json` с расширенной библиотекой шаблонов.
- Добавлены новые шаблоны для собственников, покупателей, объектов, новостроек, услуг, аренды, личного бренда и частных объявлений.
- Загрузчик шаблонов теперь собирает библиотеку из нескольких JSON-файлов.
- Дубликаты шаблонов по `id` автоматически объединяются: более поздний файл может заменить шаблон из базовой библиотеки.
- Версия состояния обновлена до `2.1.0`.

## 2.0.0

- Добавлены сценарные фильтры шаблонов.
- Появились быстрые сценарии: «Все», «Подъезд», «Собственники», «Покупатели», «Объекты», «Новостройки», «Частное», «С фото», «Экономно».
- Фильтры работают поверх выбранной задачи и поиска.
- В статусе предпросмотра теперь отображается выбранный сценарий.
- Это первый шаг к группировке шаблонов по реальным рабочим ситуациям СПН.

## 1.9.0

- Добавлено избранное для шаблонов.
- У каждого шаблона появилась кнопка добавления или удаления из избранного.
- Добавлен фильтр «Избранные» в выборе шаблона.
- Избранные шаблоны хранятся в браузере отдельно от макетов.
- СПН может быстро собрать личную подборку рабочих шаблонов.

## 1.8.0

- Добавлено управление порядком блоков макета.
- Теперь блоки можно поднимать и опускать кнопками вверх и вниз.
- Добавлена кнопка «Сбросить порядок».
- Рендер макета теперь строится по пользовательскому порядку блоков.
- Порядок блоков сохраняется в именованных макетах и JSON-файлах.

## 1.7.0

- Добавлен произвольный дополнительный текстовый блок.
- Дополнительный блок можно включать и выключать в составе макета.
- Добавлены поля: название дополнительного блока и текст дополнительного блока.
- Блок подходит для условий, ипотеки, торга, срочности, бонусов и важных уточнений.
- Проверка качества теперь учитывает пустой или слишком длинный дополнительный блок.

## 1.6.0

- Добавлено сохранение нескольких макетов с названиями.
- Добавлен список сохранённых макетов.
- Добавлены действия: сохранить как макет, загрузить выбранный, удалить выбранный.
- Именованные макеты хранятся в браузере отдельно от последнего автосохранения.
- Фото не сохраняются в именованные макеты, чтобы не переполнять localStorage.

## 1.5.0

- Добавлены настройки печати: линии реза, безопасные поля, режим проверки печати.
- Предпросмотр А4 теперь умеет показывать зоны реза и безопасную область внутри каждого макета.
- Проверка качества теперь подсказывает включить линии реза и безопасные поля.
- Окно печати дополнено чек-листом: A4, масштаб 100%, фоновые изображения, проверка QR.

## 1.4.0

- Добавлен настоящий QR-код вместо визуальной заглушки.
- QR генерируется прямо в браузере, без сервера и внешних API.
- Добавлен модуль `assets/js/qr.js`.
- Добавлена проверка слишком длинных ссылок для QR.
- Добавлены стили для печатного QR-кода.

## 1.3.0

- Добавлен модуль `assets/js/layoutRules.js` для умной подстройки макета.
- Добавлены быстрые режимы макета: «Читаемо», «Экономно», «С фото», «Витрина», «Подъезд», «Частное».
- Добавлена кнопка «Подстроить автоматически».
- Добавлены подсказки по составу макета.
- Автоподстройка меняет количество макетов на А4, плотность, фото, QR, отрывные телефоны, размеры заголовка и телефона.

## 1.2.0

- Убраны быстрые кнопки районов: район теперь вводится вручную.
- Добавлен блок «Состав макета».
- Можно включать и выключать: заголовок, цену, описание, параметры, преимущества, фото, QR, контакты.
- Рендер макета перестраивается под включенные блоки.
- Проверка качества теперь учитывает скрытые блоки.

## 1.1.0

- Добавлено сохранение профиля СПН отдельно от макета.
- Добавлены кнопки «Сохранить профиль СПН», «Подставить профиль», «Очистить объект».
- Добавлены быстрые типы объектов.
- При выборе нового шаблона имя и телефон СПН больше не сбрасываются.

## 1.0.0

- Первая модульная версия под GitHub Pages.
- Простая работа для СПН.
- Гибкая настройка для опытных пользователей.
- Шаблоны в JSON.
- Поддержка фото 1/2, QR, деления А4, контроля качества.

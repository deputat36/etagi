# Контрольный CI фото-компоновок

Дата: 12 июля 2026 года.

Цель контрольного pull request — запустить полный статический validate и настоящий browser smoke для актуального состояния `main`.

Проверяемые новые сценарии:

- `photo_left`: фото слева, текст справа;
- `photo_left`: ограничение до 2 макетов на А4;
- `photo_card`: контрастный заголовок поверх обычного фото;
- `photo_card`: безопасная планировка без overlay и обрезки;
- `photo_card`: ограничение до 2 макетов на А4;
- сохранение ранее подтверждённых сценариев `newbuild_visual` и `agent_brand_photo`.

Документ не меняет runtime и не предназначен для объединения в `main`.

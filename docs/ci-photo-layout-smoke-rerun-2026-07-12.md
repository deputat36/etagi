# Повторный контрольный CI фото-компоновок

Дата: 12 июля 2026 года.

Проверяется актуальный `main` после устранения гонки асинхронной загрузки изображений в browser smoke.

Ожидается подтверждение сценариев:

- `photo_left`: две колонки и ограничение 2 на А4;
- `photo_card`: контрастный заголовок на обычном фото;
- `photo_card`: планировка без overlay и обрезки;
- `photo_card`: ограничение 2 на А4;
- `newbuild_visual` и `agent_brand_photo` без регрессий.

Документ служит только триггером pull-request workflow и не предназначен для объединения.

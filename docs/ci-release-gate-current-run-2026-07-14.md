# Контрольный CI актуального RC gate

Служебный документ для запуска текущего `main`.

Критерии:

- release-validator извлекает положительный номер последнего workflow run из RC-документа;
- номер не зашит навечно в исходный код;
- `manual-print-test-pack-3.86.0.md` остаётся обязательным;
- статус 3.86.0 остаётся `DRAFT` до физической приёмки;
- validate, browser-smoke и печатная матрица проходят.

PR служебный. Полезный код уже находится в `main`.

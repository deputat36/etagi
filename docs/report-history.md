# История отчётов после расклейки

Документ для менеджера и СПН.

## Зачем нужна история

Один отчёт показывает результат одной расклейки. История помогает понять, какие связки работают лучше:

```text
шаблон → office-сценарий → место → формат А4 → качество → отклики → целевые обращения → следующий шаг
```

После накопления минимум двух отчётов генератор показывает:

```text
Лучше повторить
Нужно изменить
```

Дополнительно генератор объединяет повторные расклейки одного шаблона в одном месте и показывает устойчивость связок, а не только лучший единичный результат.

## Что сохраняется

Новые отчёты сохраняют:

- дату и место расклейки;
- `templateId` и название шаблона;
- цель макета;
- office-сценарий;
- office-уровень и риск;
- был ли шаблон рекомендован офисом;
- режим интерфейса: новичок, быстро или расширенно;
- оценку качества и её статус;
- до пяти главных замечаний качества;
- прогресс проверки менеджера;
- отметку о завершённой проверке менеджера;
- комментарий менеджера;
- количество листов и объявлений;
- звонки и сообщения;
- целевые обращения;
- проценты отклика и целевых обращений;
- автоматический вывод;
- следующий шаг;
- заметки.

Старые записи без новых полей продолжают открываться. Миграция localStorage не требуется.

История хранится локально в браузере пользователя и не отправляется на сервер.

## Как пользоваться

1. Выбрать шаблон и подготовить макет.
2. Нажать `Проверить`, чтобы в отчёт попала оценка качества.
3. При необходимости заполнить проверку менеджера.
4. После расклейки заполнить дату, место, листы, звонки и целевые обращения.
5. Посмотреть автоматический вывод.
6. Нажать `В историю`.
7. Использовать поиск и фильтры для сравнения шаблонов, сценариев и районов.
8. При необходимости выгрузить CSV.
9. Для повторной расклейки нажать `Повторить`.
10. Ошибочно сохранённую запись удалить отдельной кнопкой `Удалить` после подтверждения.
11. Для контроля отдела открыть `Сводку менеджеру` и выбрать период.

## Оценка качества

В отчёт сохраняется число от 0 до 100 и статус:

```text
not_checked       проверка не запускалась
good              80–100
needs_attention   60–79
critical          ниже 60
```

Также сохраняются заголовки главных ошибок и важных замечаний. Это позволяет отличить слабый отклик из-за места расклейки от слабого отклика макета, который изначально имел проблемы с телефоном, плотностью или формулировками.

## Office-контекст

Если у выбранного шаблона есть `office`-метаданные, отчёт сохраняет:

```text
officeScenario
officeLevel
officeRisk
officeRecommended
```

Это позволяет сравнивать не только заголовки, но и реальные рабочие сценарии: подъезд, собственники, объект, новостройка, ипотека, оценка и другие.

## Автоматический вывод

Генератор подсказывает следующий смысловой шаг:

- если откликов нет — проверить место, шаблон, заголовок и телефон;
- если отклики есть, но целевых нет — уточнить текст, аудиторию и office-сценарий;
- если есть первые целевые — повторить тест в другой локации;
- если результат хороший — повторить этот шаблон и сценарий в похожих домах.

## Аналитика связок

Блок `Аналитика связок` считается по сохранённой истории.

Он показывает:

- общий объём объявлений;
- отклики и целевые обращения;
- общий процент отклика;
- общий процент целевых;
- связку `Лучше повторить`;
- связку `Нужно изменить`.

Базовая связка состоит из:

```text
шаблон + templateId + office-сценарий + место + формат А4 + качество + отклик + целевые обращения
```

Старые записи без `templateId` используют заголовок макета как fallback.

`Лучше повторить` выбирается по суммарной оценке отдельного отчёта: важнее всего целевые обращения, затем процент целевых, количество откликов и общий процент отклика.

`Нужно изменить` — слабый отдельный результат с малым количеством звонков, целевых или низким процентом отклика.

## Устойчивость связок

Отдельный блок `Устойчивость связок` защищает от поспешных выводов по одной удачной расклейке.

В него попадают только группы, где сохранено минимум 2 отчёта с одинаковыми:

```text
templateId или названием шаблона + нормализованным местом расклейки
```

Для каждой группы суммируются:

- количество повторных расклеек;
- объявления;
- звонки и сообщения;
- целевые обращения;
- общий процент отклика;
- общий процент целевых;
- средняя оценка качества, если она была сохранена.

Результаты выводятся как:

```text
Стабильно лучше
Стабильно слабее
```

Так единичный сильный отчёт остаётся полезным, но не считается устойчивой связкой до повторного подтверждения в том же месте.

## Сводка менеджеру

Блок `Сводка менеджеру` собирает короткую управленческую картину по всей локальной истории.

Доступны периоды:

```text
7 дней
30 дней
Вся история
```

Рекомендуемое применение:

- `7 дней` — оперативный контроль текущей недели;
- `30 дней` — рабочая динамика и сравнение повторных действий;
- `Вся история` — долгосрочная картина по накопленным данным.

Сводка показывает:

- количество отчётов;
- общее количество листов;
- отклики и целевые обращения;
- общий процент отклика;
- среднюю оценку качества;
- сколько отчётов прошло полную проверку менеджера;
- сколько отчётов относятся к среднему или высокому риску;
- сколько расклеек сохранено без запуска контроля качества;
- лучшую устойчивую связку;
- слабую устойчивую связку;
- лучший отдельный отчёт.

Кнопка `Скопировать` формирует текст:

```text
СВОДКА ПО РАСКЛЕЙКЕ
```

Его можно отправить менеджеру, использовать на планёрке или сохранить в рабочий отчёт.

Для периодов 7 и 30 дней используются `createdAt` или распознанная календарная дата. Старые записи без читаемой даты остаются доступными в режиме `Вся история`.

## Удаление ошибочного отчёта

У каждой видимой записи есть кнопка `Удалить`.

Порядок работы:

1. Нажать `Удалить` у ошибочной записи.
2. Проверить дату и место в диалоге подтверждения.
3. Подтвердить удаление.
4. Убедиться, что история, устойчивая аналитика и сводка менеджеру пересчитались.

Удаляется только выбранная запись. Текущий черновик отчёта, профиль СПН, макеты и остальные записи не затрагиваются.

Отмена после подтверждения не предусмотрена, поэтому перед удалением нужно проверить дату и место. При сомнении сначала выгрузить CSV.

## Фильтр истории

Поиск работает по:

- дате;
- месту;
- заголовку;
- `templateId`;
- названию шаблона;
- цели;
- office-сценарию;
- office-уровню и риску;
- статусу и замечаниям качества;
- заметкам;
- следующему шагу;
- автоматическому выводу.

Кнопки:

```text
Все / Рабочие / Слабые
```

`Рабочие` — минимум 2 целевых обращения или высокий процент целевых.

`Слабые` — нет откликов или нет целевых обращений.

Обычная и устойчивая аналитика пересчитываются по текущему фильтру. Сводка менеджеру использует весь выбранный календарный период и не зависит от поисковой строки.

## CSV

CSV содержит технические и офисные поля:

```text
template_id
template_title
goal
office_scenario
office_level
office_risk
ui_mode
quality_score
quality_status
quality_warnings
manager_review
manager_reviewed
```

После них идут заголовок, листы, объявления, отклики, целевые, проценты, вывод, следующий шаг и заметки.

## Как читать результаты

Если откликов нет:

- проверить качество сохранённого макета;
- изменить место;
- усилить заголовок;
- проверить телефон;
- попробовать другой шаблон или формат А4.

Если звонки есть, но целевых мало:

- уточнить аудиторию;
- проверить office-сценарий;
- добавить конкретику по дому, району, цене или объекту;
- убрать слишком широкие обещания.

Если целевые обращения есть:

- повторить тот же `templateId` в похожих домах;
- сохранить связку как рабочую;
- дождаться минимум двух сопоставимых отчётов перед выводом об устойчивости;
- при стабильном результате перевести шаблон в рекомендованные.

## Совместимость

Старые записи без `templateId`, office-контекста и качества продолжают участвовать в истории через fallback по заголовку. В устойчивой группировке они объединяются по заголовку и месту.

## Ограничения

История хранится в `localStorage`. Если пользователь очистит данные браузера, история пропадёт. Для офисного контроля нужно периодически выгружать CSV. Полный backup всех локальных данных запланирован отдельным этапом roadmap.
